# Leave VITE_API_URL empty for the Netlify deploy — the netlify.toml redirect
# forwards /api/* to the serverless function on the same origin.
#
# For the Capacitor Android build, set this to the absolute Netlify URL:
#   VITE_API_URL=https://your-site.netlify.app
VITE_API_URL=
